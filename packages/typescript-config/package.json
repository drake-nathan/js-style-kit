{
  "name": "@repo/typescript-config",
  "private": true,
  "scripts": {
    "format": "prettier . --write --cache",
    "format:check": "prettier . --check --cache"
  },
  "dependencies": {
    "typescript": "^5.8.3"
  }
}
